/* Injectée par `define` dans `vite.config.ts`, lue sur `package.json`. Déclarée
   ici plutôt qu'importée : le manifeste npm n'a pas à entrer dans le bundle
   pour qu'un numéro de version s'affiche. */
declare const __APP_VERSION__: string
